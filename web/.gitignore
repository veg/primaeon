# Build output and SvelteKit's generated types
node_modules
build
.svelte-kit
vite.config.ts.timestamp-*

# Written by ../scripts/copy-assets.mjs on every build/dev. Never commit: the ORT binary is 13 MB
# of third-party output pinned by package.json and the models come from the engine repo's
# manifest. See the script header for why each is copied. (static/wasm/ was the HyPhy bundle,
# removed with PLAN.md D22 in Phase 3; nothing writes it any more.)
static/ort/
static/models/
static/_headers
