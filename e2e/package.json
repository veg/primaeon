{
	"name": "@veg/hyphaeon-e2e",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"description": "Playwright delivery assertions for the HyphAeon web app: origins, headers, bytes per route.",
	"scripts": {
		"e2e": "playwright test",
		"e2e:headed": "playwright test --headed",
		"install-browsers": "playwright install chromium"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.0",
		"@types/node": "^22.0.0",
		"typescript": "^5.9.0"
	}
}
